# JobCompass 求职指南针 产品需求文档（PRD）

## 0. 文档概述

| 项目 | 内容 |
| --- | --- |
| 文档名称 | JobCompass 求职指南针 产品需求文档 |
| 版本 | v2.0 |
| 状态 | 待评审 |
| 适用阶段 | 第一版开发与验收 |
| 更新日期 | 2026-09-13 |
| 覆盖范围 | 前端页面、后端服务、数据库、AI 生成能力 |

### 0.1 阅读对象

产品、设计、前端、后端、测试，以及参与评审的业务方。

### 0.2 本期范围

本文档描述第一版的完整需求，包含已经实现的代码逻辑的标准化定义。第二版及以后的功能只在第 10 章列出范围，不做详细定义。

### 0.3 术语表

| 术语 | 含义 |
| --- | --- |
| 岗位 | 用户想要求职的目标职位，例如产品经理、数据分析师 |
| 成长路径 | 系统为某个岗位生成的一整套内容，含岗位要求、学习路径、面经、课程 |
| 阶段 | 学习路径中的一级分组，包含任务、交付物、验收标准 |
| 面经 | 真实的面试题目及答题思路 |
| 可信度 | 面经来源的可靠程度，分已核实真题、社区经验、AI 整理三级 |
| 访客 | 未登录状态的用户 |
| 会员 | 完成订阅的用户 |

---

## 1. 背景与目标

### 1.1 背景

在校生准备求职时，最大的障碍不是缺少资料，而是资料太散、没有标准、无法判断真假。岗位要求分散在招聘网站，经验帖分散在小红书，课程分散在 B 站，面试题分散在牛客。用户把这些信息拼起来之后，仍然不知道先学什么、学到什么程度算入门。

大模型和检索增强技术已经可以把这些非结构化内容整理成按岗位组织的结构化知识。市面上已有的产品集中在简历优化、模拟面试和题库三个环节，针对入门准备阶段的成长路径产品较少，专门深耕单一岗位的就更少。

### 1.2 用户调研

第一轮完成 5 位目标用户访谈。前 4 位为软件与通用岗位方向，第 5 位来自电子信息专业，代表硬科技与工科方向。以下为第 5 位访谈的完整记录。

**同学 E，电子信息专业**

第一，本专业最经典的方向是 PCB 电路设计，另外还有硬件工程师方向。这些方向比较依赖经验积累，而本人的比赛和实习经历还不够多，企业很少愿意招实习生从头培养，所以需要自己学习相关知识、积累项目经验。最难的点是项目实践太少，哪怕有学习方向，也不够清晰，很难判断自己学到什么程度才算真正入门。

第二，对本专业的就业情况并没有太深入的了解，一开始想过做自媒体，后来觉得不现实，还是决定踏实学习专业内容，比如嵌入式、电路方向，并尝试做一些相关的实践项目，慢慢摸索适合自己的路。

第三，这类工具是绝大多数应届生最需要的东西。如果进一步和就业实际接轨，甚至得到企业支持，对还在摸索方向的人会特别有帮助。

第四，学校辅导员会提供一些就业指导，前辈的就业经历也可以参考，此外还会自己在 B 站、AI 等渠道搜集资料。最难的点是不确定这些信息能否实实在在提升就业竞争力，对付费课程也抱有怀疑，怕学完还是找不到方向。

第五，AI 生成的学习路径只能作为参考，具体还要靠专业知识和经验判断，也可以向前辈请教核实。保持理性判断，把它当作辅助工具即可。

**本轮访谈得到的新结论**

一是痛点从软件岗位扩展到工科与传统行业。电子信息、硬件、嵌入式方向的用户同样缺少清晰路径，而且企业更不愿意培养零经验实习生，自学和实践的压力更大。

二是"不知道学到什么程度算入门"这一诉求第三次出现。达标标准是本产品的核心价值点，需要在所有岗位内容中强制保留。

三是用户对付费内容天然怀疑，信任问题比功能丰富度更重要。

四是用户明确提出希望与真实就业接轨，甚至引入企业或从业者背书。这是后续版本的重要方向。

五是用户对 AI 输出保持理性预期，把他当作参考而非答案。这一点决定了产品必须提供来源标注，让用户能自行核实。

### 1.3 产品目标

让零基础的学生在输入目标岗位后，十分钟内拿到一份能照着执行的成长路径，替代在碎片信息中反复搜索的过程。

### 1.4 关键结果

| 编号 | 关键结果 | 验证方式 |
| --- | --- | --- |
| KR1 | 用产品经理岗位跑通从输入到输出的完整链路 | 端到端验收 |
| KR2 | 5 位目标用户试用，至少 3 位愿意保存或转发路径 | 用户测试 |
| KR3 | 学习路径每个阶段都有交付物和验收标准 | 内容抽检 |
| KR4 | 每条面经都带来源和可信度标记 | 内容抽检 |

---

## 2. 用户角色

### 2.1 目标用户画像

| 画像 | 代表用户 | 核心诉求 |
| --- | --- | --- |
| 目标明确型转岗生 | 小陈，大四，目标产品经理 | 想要一套从零到能投产品实习的具体行动方案 |
| 有方向但卡在技能对标 | 小周，大四，目标数据分析或运营 | 想把岗位要求拆成必会与加分，并知道自己差在哪 |
| 求稳型求职者 | 小林，大四，倾向熟悉或上手难度小的岗位 | 想在投入前确认岗位适不适合自己 |
| 工科方向探索者 | 小韩，电子信息专业 | 想判断学到什么程度算入门，并希望内容与真实就业接轨 |

第一版重点服务小陈和 AI 产品经理方向，第二版扩展到小韩代表的工科方向。

### 2.2 角色与权限矩阵

| 功能 | 访客 | 登录用户 | 会员 |
| --- | --- | --- | --- |
| 输入岗位并生成路径 | 可用 | 可用 | 可用 |
| 查看岗位要求 | 可用 | 可用 | 可用 |
| 查看学习路径与打勾 | 可用 | 可用 | 可用 |
| 查看课程推荐 | 可用 | 可用 | 可用 |
| 查看面经 | 仅 1 条（路径页）／3 条（题库页） | 基础面经 | 全部面经 |
| 保存成长路径 | 不可用，触发登录 | 可用 | 可用 |
| 我的计划 | 不可用，触发登录 | 可用 | 可用 |
| 查看术语知识库 | 可用 | 可用 | 可用 |
| 查看标准答题思路与来源链接 | 不可用 | 部分 | 可用 |
| 导出与分享 | 不可用 | 不可用 | 可用 |
| 按公司、年份筛选面经 | 可用 | 可用 | 可用 |

---

## 3. 核心需求

### 3.1 需求清单

| 编号 | 需求 | 优先级 | 来源 |
| --- | --- | --- | --- |
| R1 | 输入岗位，生成完整成长路径 | P0 | 全部访谈 |
| R2 | 岗位要求拆解，区分必会与加分，附达标标准 | P0 | 小周、小韩 |
| R3 | 分阶段学习路径，含任务、交付物、验收标准 | P0 | 小陈、小周、小韩 |
| R4 | 面经题库，带来源与可信度标注 | P0 | 小陈、小周 |
| R5 | 课程推荐，说明学习理由 | P1 | 小陈 |
| R6 | 术语知识库，支持分类与搜索 | P1 | 小陈 |
| R7 | 保存成长路径，支持回访续学 | P1 | 调研结论"看了一堆记不住" |
| R8 | 按公司、年份筛选面经 | P1 | 小周 |
| R9 | 登录与会员分层，面经作为付费点 | P2 | 商业探索 |
| R10 | 组队打卡学习 | P2 | 留存探索 |

### 3.2 需求边界

第一版不做：用户注册的完整账号体系、支付接入、真实爬虫采集、RAG 检索、多语言。

---

## 4. 产品架构

### 4.1 信息架构

```
JobCompass
├── 首页
│   ├── 岗位输入
│   ├── 热门岗位快捷选择
│   └── 求职背景多选
├── 成长路径页（生成后进入）
│   ├── 岗位要求
│   ├── 学习路径
│   ├── 基本面经
│   └── 课程推荐
├── 我的计划
├── 资料库
├── 面经题库
└── 知识库
```

### 4.2 系统架构

| 层级 | 实现 | 说明 |
| --- | --- | --- |
| 前端 | 单文件网页，原生 HTML、CSS、JavaScript | 无构建步骤，双击可打开 |
| 后端 | FastAPI | 提供接口并托管前端页面 |
| 数据库 | SQLite | 文件型数据库，零配置 |
| AI | DeepSeek | 结构化 JSON 输出 |

前端有两种运行模式。通过后端访问时，页面在启动时请求 `/api/bootstrap` 获取真实数据；直接打开本地文件时，使用页面内置数据，保证离线可用。

### 4.3 数据流程

```
采集 → 清洗 → 校验 → 入库 → 检索 → 生成 → 输出
```

第一版中，采集与清洗由人工完成，AI 生成环节已实现，检索环节尚未实现，属于第二版范围。

---

## 5. 功能详细需求

### 5.1 M1 首页与岗位生成

**功能描述**

用户输入目标岗位并勾选自身情况，点击生成后进入成长路径页。

**输入**

- 岗位名称，文本框，最长 30 字
- 热门岗位快捷标签
- 求职背景多选标签，默认选中"大四 / 应届"与"零产品经验"

**输出**

- 加载动画，依次展示 4 条进度提示，最短展示 2.4 秒
- 生成成功后跳转成长路径页

**前置条件**

无。访客即可使用。

**后置条件**

当前岗位写入前端状态，路径页展示对应内容。

**交互规则**

- 点击热门岗位标签可覆盖输入框内容
- 背景标签支持多选与取消
- 点击"更多"展开额外 4 个背景选项，再次点击收起
- 岗位名包含"AI"或"人工智能"时，默认匹配 AI 产品经理方向

**异常场景**

| 场景 | 处理 |
| --- | --- |
| 岗位名称为空 | 仍可使用默认岗位生成，不出现报错中断 |
| 后端不可用 | 使用页面内置数据完成展示 |
| 模型调用失败 | 后端返回内置数据并附带提示，前端控制在控制台输出 |
| 生成超时 | 后端超时时间 120 秒，超时后走降级逻辑 |

**权限说明**

访客可用，不消耗次数限制。

### 5.2 M2 成长路径页

**功能描述**

集中展示某个岗位的全部内容，是产品的核心页面，包含四个子模块。

**输入**

岗位标识，来自首页生成结果或"我的计划"中的已保存记录。

**输出**

- 岗位头部：岗位名称、一句话说明、岗位简介，以及三个统计值（学习阶段数、面经题数、路径完成度）
- 岗位要求：必会技能、加分技能、达标标准
- 学习路径：整体进度条，以及每个阶段的任务清单、交付物、验收标准、学习资源链接
- 基本面经：默认展示 1 条，其余锁定
- 课程推荐：课程标题、平台、难度、学习理由，点击跳转 B 站搜索

**前置条件**

已完成一次岗位生成，或从"我的计划"进入。

**后置条件**

任务打勾状态保存在前端状态；点击保存后写入数据库。

**交互规则**

- 四个子模块通过页内标签切换，默认停在岗位要求
- 任务点击切换完成状态，完成后文字加删除线
- 进度条按已完成任务占全部任务的比例计算并四舍五入
- 面经条目点击展开答题思路
- 资源链接新窗口打开
- 点击"换个岗位"清空当前进度并返回首页
- 保存按钮在未登录时触发登录弹窗，已登录时写入"我的计划"并跳转

**异常场景**

| 场景 | 处理 |
| --- | --- |
| 岗位内容为空 | 展示空状态提示，不出现空白页面 |
| 保存接口失败 | 保持页面状态，提示保存失败 |
| 重复保存同一岗位 | 覆盖已有记录，不产生重复条目 |

**权限说明**

访客可查看全部岗位要求、学习路径与课程。面经默认仅展示 1 条。

### 5.3 M3 面经题库

**功能描述**

独立的面经浏览与筛选页面，定位为可检索的题库。

**输入**

- 岗位筛选，下拉选择
- 公司筛选，下拉选择
- 年份筛选，下拉选择
- 关键词搜索（接口已支持，界面暂未开放）

**输出**

- 筛选后的面经列表，每条包含可信度标签、标签、来源、题目、答题思路
- 结果计数

**前置条件**

无。

**后置条件**

筛选条件保存在前端状态，刷新后重置。

**交互规则**

- 三个筛选项互相独立，可任意组合
- 结果为空时展示空状态文案
- 排序规则为年份倒序

**异常场景**

| 场景 | 处理 |
| --- | --- |
| 筛选结果为空 | 展示"换个条件试试" |
| 接口失败 | 使用内置数据 |

**权限说明**

访客可查看前 3 条，其余锁定。会员解锁全部。

### 5.4 M4 知识库

**功能描述**

产品与技术术语的解释页面，帮助零基础用户快速建立概念。

**输入**

- 关键词搜索，支持匹配术语名与解释
- 分类筛选，含产品概念、技术概念、流程术语、数据指标、求职术语

**输出**

- 术语卡片列表，每条含术语名、分类、解释、示例、相关术语
- 结果计数

**前置条件**

无。

**后置条件**

搜索词与分类保存在前端状态。

**交互规则**

- 搜索为即时过滤，输入即刷新结果
- 搜索框在刷新后保持焦点与光标位置
- 分类与搜索词同时生效

**异常场景**

| 场景 | 处理 |
| --- | --- |
| 无匹配结果 | 展示"没找到这个术语，换个词试试" |

**权限说明**

全部免费开放，不设限制。

### 5.5 M5 资料库

**功能描述**

按技能分类的学习资源库。

**输入**

技能分类筛选。

**输出**

课程卡片列表，含标题、平台、难度、所属分类、学习理由，点击跳转 B 站搜索。

**前置条件**

无。

**后置条件**

分类选择保存在前端状态。

**异常场景**

| 场景 | 处理 |
| --- | --- |
| 分类下无内容 | 展示空状态提示 |

**权限说明**

全部免费开放。

### 5.6 M6 我的计划

**功能描述**

管理已保存的成长路径，是用户回访的入口。

**输入**

已保存的计划记录。

**输出**

- 计划列表：岗位名称、保存日期、完成进度
- 每项操作：继续学习、删除
- 空状态：提示生成或登录

**前置条件**

已登录并至少保存过一次路径。

**后置条件**

点击"继续学习"载入该岗位内容与历史打勾状态。

**交互规则**

- 同一岗位只保留一条记录，重复保存时覆盖
- 进度以保存时的完成度为准

**异常场景**

| 场景 | 处理 |
| --- | --- |
| 未保存任何计划 | 展示空状态与引导按钮 |
| 本地存储不可用 | 忽略写入错误，页面继续可用 |

**权限说明**

仅登录用户可用。

### 5.7 M7 账户与权限

**功能描述**

第一版实现访客、登录用户、会员三种状态的前端切换，用于验证注册钩子与付费意愿，不接入真实账号体系与支付。

**输入**

登录弹窗或订阅弹窗中的确认操作。

**输出**

导航栏状态变化。登录后显示"生成新路径"，会员额外显示会员标记。

**交互规则**

- 未登录时导航栏展示"登录"入口
- 触发保存路径时，未登录用户先看到登录弹窗
- 点击锁定面经的解锁按钮时，展示订阅弹窗
- 弹窗点击遮罩或"以后再说"关闭

**异常场景**

| 场景 | 处理 |
| --- | --- |
| 用户关闭弹窗 | 不执行任何操作，回到原页面 |

**权限说明**

仅前端状态模拟，刷新后重置。

### 5.8 M8 AI 生成引擎

**功能描述**

根据岗位名称调用大模型生成完整内容，并写入数据库。

**输入**

- 岗位名称
- 用户勾选的求职背景

**输出**

符合固定结构的岗位内容，包含岗位信息、技能、达标标准、学习阶段、面经、课程。

**前置条件**

后端配置了有效的模型密钥。

**后置条件**

生成结果写入数据库，同名岗位再次请求时直接读取，不重复调用模型。

**交互规则**

- 模型必须返回 JSON，后端再做字段校验与补全
- 缺少的字段用默认值补齐，避免前端渲染异常
- 课程链接统一生成为 B 站搜索链接
- 可信度字段只允许 high、medium、ai 三个值

**异常场景**

| 场景 | 处理 |
| --- | --- |
| 未配置密钥 | 使用内置数据，返回提示信息 |
| 模型返回非法 JSON | 使用内置数据 |
| 网络不可达或超时 | 使用内置数据 |
| 返回字段缺失 | 按默认值补齐后返回 |

**权限说明**

访客可用。成本控制依赖同名岗位缓存。

---

## 6. 交互规则

### 6.1 全局规范

- 所有页面通过顶部导航切换，当前页高亮
- 页面切换后回到顶部
- 一级操作按钮使用主题红色，次级操作使用描边按钮
- 数据来源标注分为三级：已核实真题、社区经验、AI 整理
- 空状态必须给出下一步引导，不出现纯空白页面

### 6.2 反馈规范

| 场景 | 反馈方式 |
| --- | --- |
| 生成中 | 进度动画，含 4 条状态提示 |
| 操作成功 | 直接展示结果，不额外弹提示 |
| 需要登录 | 弹窗说明原因与收益 |
| 需要订阅 | 弹窗说明解锁范围 |
| 接口失败 | 使用降级数据，用户无感知 |

### 6.3 降级规范

后端不可用、模型不可用、数据为空三种情况都必须保证页面可浏览，不允许出现白屏或错误堆栈。

---

## 7. 页面流程

### 7.1 主流程

```
进入首页
→ 输入目标岗位、勾选背景
→ 点击生成
→ 加载动画
→ 成长路径页（默认岗位要求）
→ 切换查看学习路径、面经、课程
→ 任务打勾
→ 点击保存
→ 未登录则登录
→ 保存成功
→ 我的计划
```

### 7.2 回访流程

```
进入首页
→ 点击我的计划
→ 选择一条计划
→ 继续学习
→ 成长路径页，恢复历史进度
```

### 7.3 探索流程

```
顶部导航
→ 面经题库（按岗位、公司、年份筛选）
→ 知识库（按分类筛选或搜索术语）
→ 资料库（按技能分类浏览课程）
```

---

## 8. 数据埋点

第一版尚未接入埋点，以下为需要补充的事件定义，用于验证留存与转化。

| 事件名 | 触发时机 | 关键参数 | 用途 |
| --- | --- | --- | --- |
| page_view | 页面切换 | page_id、role_id | 页面流量分布 |
| generate_click | 点击生成 | keyword、background | 生成意愿 |
| generate_success | 生成完成 | role_id、source、duration | 生成成功率与耗时 |
| generate_fallback | 走降级逻辑 | reason | 稳定性监控 |
| tab_switch | 切换路径页子模块 | role_id、tab_id | 内容偏好 |
| task_check | 任务打勾 | role_id、stage_index、task_index | 进度行为 |
| save_click | 点击保存 | role_id、logged_in | 注册钩子转化 |
| login_success | 登录完成 | trigger | 注册转化来源 |
| subscribe_click | 点击解锁面经 | role_id | 付费意愿 |
| interview_view | 展开答题思路 | question_id | 面经消费深度 |
| term_search | 知识库搜索 | keyword、result_count | 内容覆盖缺口 |
| plan_revisit | 从我的计划继续学习 | role_id | 留存 |

核心指标：每周成功生成并保存路径的独立用户数。辅助指标：生成成功率、保存转化率、7 日回访率、知识库搜索无结果率。

---

## 9. 非功能需求

### 9.1 性能

| 项目 | 要求 |
| --- | --- |
| 页面首屏 | 本地环境 1 秒内完成渲染 |
| 接口响应 | 数据查询类接口 200 毫秒内返回 |
| AI 生成 | 单次生成 60 秒内返回，超过 120 秒判定超时 |
| 加载反馈 | 超过 300 毫秒的操作必须给出反馈 |

### 9.2 安全

- 模型密钥只存放在本地配置文件，不进入代码与版本库
- 版本库排除密钥文件、数据库文件与依赖目录
- 前端不接收任何用户个人信息
- 接口不提供删除全部数据的操作
- 采集他人内容时只做摘要并保留原文链接，不整篇转载

### 9.3 兼容性

| 项目 | 要求 |
| --- | --- |
| 浏览器 | Chrome、Edge 最近两个大版本 |
| 屏幕宽度 | 支持 375 像素到 1440 像素 |
| 移动端 | 布局自适应，导航横向滚动 |
| 运行环境 | Windows 优先，后端不依赖特定操作系统 |
| 数据库 | SQLite 单文件，支持平滑迁移到 PostgreSQL |

### 9.4 可用性

- 断网或接口异常时页面仍可浏览
- 所有列表为空时提供引导文案
- 重要操作（保存、删除）有明确的结果反馈

---

## 10. 上线范围

### 10.1 第一版

已经实现并纳入验收的范围：

- 五个页面与成长路径页四个子模块
- 岗位生成链路，含 AI 生成与降级
- 面经、知识库、资料库三类内容的筛选
- 保存路径与我的计划
- 访客、登录、会员三态权限
- 后端十五个接口与十张数据表
- 内置两个岗位、十一条面经、十门课程、二十九条术语

### 10.2 第二版

- 检索增强生成，生成时引用术语库与面经库内容并标注来源
- 真实数据采集管道
- 技能差距自评
- 接入真实账号体系与支付
- 埋点与数据看板

### 10.3 第三版

- 组队打卡
- 用户提交面经
- 企业或从业者内容背书
- 扩展到工科与硬件方向岗位

---

## 11. 排期与风险

### 11.1 排期

| 阶段 | 内容 | 相对周期 |
| --- | --- | --- |
| 已完成 | 调研、竞品分析、PRD、原型、后端、AI 接入 | — |
| 第二版第一批 | 检索增强生成、真实数据采集 | 2 到 3 周 |
| 第二版第二批 | 技能差距自评、账号与支付 | 2 到 3 周 |
| 第三版 | 组队打卡与内容生态 | 视验证结果而定 |

### 11.2 风险

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 内容来源不权威 | 用户不信任，面试时也经不起追问 | 引入从业者校验，标注来源与可信度 |
| 数据量不足 | 产品价值依赖内容量 | 优先做深单一岗位，再扩量 |
| 求职属一次性需求 | 留存低 | 设计回访入口，验证打卡与提醒 |
| 用户不愿付费 | 商业模式不成立 | 第一版不收费，先用访谈验证意愿 |
| 采集内容涉及版权 | 合规风险 | 只做摘要并保留链接，不做整篇转载 |
| 模型输出不稳定 | 内容质量波动 | 固定输出结构，后端校验，保留降级 |

---

## 12. 接口说明

### 12.1 接口清单

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /api/health | 服务健康检查，返回岗位数量 |
| GET | /api/nav | 导航配置 |
| GET | /api/roles | 岗位列表 |
| GET | /api/roles/{role_id} | 岗位详情，含技能、标准、阶段、面经、课程 |
| GET | /api/interviews | 面经列表，支持 role、company、year、q 筛选 |
| GET | /api/interviews/filters | 面经筛选项，返回角色、公司、年份 |
| GET | /api/courses | 课程列表，支持 category、q 筛选 |
| GET | /api/courses/categories | 课程分类列表 |
| GET | /api/terms | 术语列表，支持 category、q 筛选 |
| GET | /api/terms/categories | 术语分类列表 |
| GET | /api/plans | 已保存计划列表 |
| POST | /api/plans | 保存或更新计划 |
| DELETE | /api/plans/{plan_id} | 删除指定计划 |
| GET | /api/ai/status | AI 配置状态，返回是否配置密钥与模型名 |
| POST | /api/generate | 按岗位生成成长路径 |
| GET | /api/bootstrap | 一次性返回前端所需全部数据 |
| GET | / | 返回前端页面 |
| GET | /{full_path} | 前端路由兜底，非接口路径均返回页面 |

### 12.2 关键接口详情

**POST /api/generate**

请求体：

```json
{
  "role_name": "数据分析师",
  "background": ["大四 / 应届", "会一点 Python"]
}
```

响应体：

```json
{
  "source": "ai",
  "role_id": "gen-6c51904d",
  "data": {
    "id": "gen-6c51904d",
    "name": "数据分析师",
    "mark": "DA",
    "tagline": "一句话说明",
    "summary": "岗位简介",
    "skills": {
      "required": [],
      "bonus": [],
      "standards": []
    },
    "stages": [],
    "interviews": [],
    "courses": []
  },
  "note": ""
}
```

source 取值：

| 取值 | 含义 |
| --- | --- |
| ai | 本次调用模型生成 |
| cache | 命中已有生成结果，未调用模型 |
| seed | 使用内置数据（未配置密钥、调用失败或超时） |

**POST /api/plans**

请求体：

```json
{
  "role_id": "pm",
  "progress": 25,
  "done": { "0-0": true }
}
```

同岗位重复提交时覆盖原记录。

### 12.3 数据结构

十张数据表：

| 表 | 说明 | 关键字段 |
| --- | --- | --- |
| roles | 岗位 | id、name、mark、tagline、summary、source |
| skills | 技能 | role_id、kind（required／bonus）、name、sort |
| standards | 达标标准 | role_id、content、sort |
| stages | 学习阶段 | role_id、title、duration、deliverable、standard、sort |
| stage_tasks | 阶段任务 | stage_id、content、sort |
| stage_resources | 阶段资源 | stage_id、name、url |
| interviews | 面经 | role_id、company、year、type、question、tags、source、credibility、answer |
| courses | 课程 | role_id、title、category、platform、level、reason、url |
| terms | 术语 | term、category、definition、example、related |
| saved_plans | 已保存计划 | role_id、progress、done_json、created_at |

---

## 13. 版本变更记录

| 版本 | 日期 | 变更内容 |
| --- | --- | --- |
| v1.0 | 2026-09-09 | 首版 PRD，定义产品定位、MVP 范围、技术选型与合规方案 |
| v2.0 | 2026-09-13 | 新增第 5 位用户访谈（电子信息方向）；补齐页面结构、权限矩阵、八个功能模块的完整定义；新增交互规则、页面流程、数据埋点、非功能需求、接口说明与数据结构；将已实现代码逻辑标准化，明确异常场景与降级策略 |
